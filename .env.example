# Database
DATABASE_URL=postgresql://fitlog:fitlog@localhost:5432/fitlog

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=change_me_to_random_64_chars_secret_key_abcdefghijklmnopqrstuvwxyz12
JWT_REFRESH_SECRET=change_me_to_another_random_64_chars_secret_key_abcdefghijklmnop

# Mail (Nodemailer SMTP). Если переменные не заданы — ссылки сброса пароля пишутся в лог.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="ForzaFit <no-reply@forzafit.ru>"

# App
PORT=3001
NODE_ENV=development
APP_URL=http://localhost:3000
